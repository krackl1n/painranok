services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    image: paintranok-web:latest
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      HOSTNAME: 0.0.0.0
      PORT: "3000"
    ports:
      - "3000:3000"
    restart: unless-stopped
